	// PART 1 CHECKING ARGUMENTS
		    // if smaller 2 -> error
    	// is int?
    	// is too big?
    	// are there duplicates?
	// PART 2 creating stack a and stack b || turning arguments into linked list
		// define a strucutre to represent the nodes of the linked list
			// var to hold value and pointer to next node
		// iterate (wiederholen?) through the command-line arguments and create
		// a new node for each argument, setting its value accodringly
		//Update the pointers to link the nodes together to form the linked list

        // write all the operations
            // sa
            // sb
            // ss
            // pa
            // pb
            // ra
            // rb
            // rr
            // rra
            // rrb
            // rrr

    // put everything in Stack A
        // is it already sorted
